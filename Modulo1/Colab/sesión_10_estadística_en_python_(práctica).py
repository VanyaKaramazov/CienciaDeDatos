# -*- coding: utf-8 -*-
"""Sesión 10: Estadística en Python (Práctica)

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1LcRcXIwV3bttw-s4Cqg-2KHuCFSbdurM

# Sesión 10: Estadística en Python (Práctica)

¡Bienvenidos a la décima sesión! Hoy cerramos la primera mitad de nuestro módulo y, por fin, verán la verdadera magia de la programación. Dejaremos atrás las operaciones manuales y aprenderemos a **automatizar** el análisis de datos que antes nos tomaba horas hacer a mano o en Excel.

### De la calculadora al código: Automatizando el análisis
Hacer cálculos a mano o en Excel es como cocinar plato por plato. Si tienes muchos invitados, te vas a agotar. Programar en Python, en cambio, es como enseñarle a la computadora a cocinar. Tú le escribes la receta (el código) una sola vez, y la máquina es capaz de preparar 100,000 platos en una fracción de segundo, sin equivocarse. Hoy dejaremos que Python haga el trabajo pesado por nosotros.

---

### Bibliotecas y Módulos: Nuestras cajas de herramientas
Para que Python calcule cosas complejas, no necesitamos inventar las fórmulas matemáticas desde cero. Aquí entra uno de los conceptos más poderosos de la programación: los **Módulos** (o bibliotecas).

Piensa en un módulo como si fuera una "caja de herramientas especializada" que alguien más ya construyó.
* Si vas a arreglar una tubería, sacas tu caja de plomería.
* Si vas a analizar datos, sacas tu caja de matemáticas.

En Python, contamos con el módulo `statistics`, una caja que ya contiene los desarmadores y martillos exactos para calcular promedios, medianas, modas y desviaciones estándar.

Para abrirla, solo tenemos que usar la palabra mágica `import`. Ejecuta la siguiente celda para ver la diferencia entre trabajar "a mano" (S9) y usar tu nueva herramienta:
"""

# 1. Le decimos a Python: "Tráeme la caja de herramientas de estadística"
import statistics

# Nuestra base de datos de pacientes
edades = [25, 30, 28, 35, 40, 22, 28, 31, 29, 28]

# ---------------------------------------------------------
# EL MÉTODO ANTIGUO (Sesión 9) — tú escribías la fórmula
promedio_antiguo = sum(edades) / len(edades)
print(f"Promedio calculado a mano (S9):   {round(promedio_antiguo, 2)}")

# ---------------------------------------------------------
# EL NUEVO MÉTODO (Sesión 10) — la caja lo hace por ti
promedio_nuevo = statistics.mean(edades)
print(f"Promedio automatizado (S10):      {promedio_nuevo}")
print("\n✅ Mismo resultado. Pero en S10 lo logramos en 1 línea.")

"""---

## 🆕 Mapa de Herramientas: Todo lo que tiene `statistics`

Antes de practicar, aquí está tu guía de referencia rápida. Estas son todas las funciones que usaremos hoy:

| Función | ¿Qué calcula? | Equivalente S9 (manual) |
|---|---|---|
| `statistics.mean(lista)` | Promedio (Media) | `sum(lista) / len(lista)` |
| `statistics.median(lista)` | Mediana (valor central) | `sorted(lista)[len(lista)//2]` |
| `statistics.mode(lista)` | Moda (valor más frecuente) | Diccionario contador con `for` |
| `statistics.stdev(lista)` | Desviación Estándar | Fórmula compleja de múltiples pasos |

> 💡 **Nota clave sobre `import`:** Solo necesitas escribir `import statistics` **una vez por sesión** de Colab. Si tu notebook ya lo tiene en una celda anterior, no es necesario repetirlo. Sin embargo, es buena práctica incluirlo al inicio de cada celda independiente para que funcione aunque ejecutes las celdas fuera de orden.

---

### Ejercicio 1.1: Tu primera herramienta especializada

En la sesión teórica aprendimos que la **Mediana** es el "corazón de la fila" y nos ayuda a evitar el engaño de los valores atípicos.

**Tu Tarea:**

1. En la celda de abajo, asegúrate de importar primero tu caja de herramientas (`import statistics`).
2. Tienes una lista de `salarios` donde casi todos ganan entre $9,500 y $12,000, pero hay un director que gana $500,000.
3. Usa `statistics.mean()` para calcular el promedio y guárdalo en una variable.
4. Usa `statistics.median()` para encontrar el salario central real y guárdalo en otra variable.
5. Usa `print()` con **f-strings** para mostrar ambos resultados y compara: ¿cuál representa mejor a la mayoría?
"""

salarios = [10000, 12000, 10500, 11000, 9500, 500000]

# Escribe aquí tu código para el Ejercicio 1.1:

"""---

## Parte 2: De la Teoría a la Práctica (El Módulo Statistics Completo)

En el ejercicio anterior ya vimos cómo abrir nuestra caja de herramientas usando `import statistics` y usamos un par de ellas. Ahora, vamos a sacar el **set completo** para hacer un análisis estadístico integral en un solo paso.

### Cálculo Automatizado: El Reporte Clínico

Imagina que tienes una lista con los días que tardaron en recuperarse 9 pacientes después de una cirugía de apéndice. Vamos a calcular su promedio, mediana y moda en un instante.

Ejecuta esta celda:

"""

import statistics

# Nuestra base de datos: Días de recuperación de 9 pacientes
tiempos_recuperacion = [5, 7, 4, 5, 8, 5, 6, 4, 5]

# Cálculos automáticos usando nuestra "caja de herramientas"
promedio = statistics.mean(tiempos_recuperacion)
mediana  = statistics.median(tiempos_recuperacion)
moda     = statistics.mode(tiempos_recuperacion)

print("📋 REPORTE CLÍNICO — Tiempos de recuperación post-operatoria")
print(f"  Promedio : {round(promedio, 1)} días")
print(f"  Mediana  : {mediana} días")
print(f"  Moda     : {moda} días  ← el tiempo más frecuente")

"""### ¿Qué sucedió paso a paso?

1. **Velocidad relámpago:** Internamente, al usar `statistics.mean()`, Python recorrió toda tu lista, sumó los 9 valores y los dividió en cuestión de milisegundos. Lo que a mano te tomaría un par de minutos (y es propenso a errores humanos), la computadora lo hace al instante, ¡incluso si la lista tuviera un millón de pacientes!
2. **El idioma del código:** Notarás que usamos palabras en inglés como `mean` (promedio), `median` (mediana) y `mode` (moda). En programación, el inglés es el estándar universal. Las herramientas tienen nombres descriptivos en este idioma para que cualquier científico de datos, en cualquier parte del mundo, pueda leer tu código y entender exactamente la lógica matemática que estás aplicando.

---

## 🆕 Trampa común: ¿Qué pasa cuando no hay una sola Moda?

Antes de que te enfrentes a este error en el Ejercicio 2.1, necesitas conocer una **limitación importante** de `statistics.mode()`: si dos o más valores se repiten el mismo número de veces (empate), Python lanzará un error llamado `StatisticsError`.

Ejecuta esta celda para ver el error en acción — ¡no entres en pánico, lo hacemos a propósito!
"""

import statistics

# Lista con dos modas (empate entre 5 y 7)
calificaciones_empate = [5, 7, 5, 7, 8, 9]

# Intento 1: statistics.mode() — lanzará un error
try:
    moda = statistics.mode(calificaciones_empate)
    print(f"Moda: {moda}")
except statistics.StatisticsError as e:
    print(f"⚠️  Error de Statistics: {e}")
    print("   → Esta lista tiene más de una moda (empate).")

print()

# Solución: statistics.multimode() — devuelve TODAS las modas (Python 3.8+)
todas_las_modas = statistics.multimode(calificaciones_empate)
print(f"✅ Con multimode(): las modas son {todas_las_modas}")
print("   → Úsalo cuando sospechas que puede haber empate.")

"""---

### Ejercicio 2.1: El Consultorio Pediátrico

¡Tu turno en el teclado! El área de pediatría necesita un reporte rápido sobre el peso (en kilogramos) de los últimos 7 recién nacidos.

**Tu tarea:**

1. En la celda de abajo, usa la lista `pesos_bebes` que ya te preparamos.
2. Crea tres variables (`promedio_peso`, `mediana_peso`, `moda_peso`).
3. Usa tu caja de herramientas `statistics` para calcular las tres medidas (`mean`, `median`, `mode`).
4. Usa `print()` con **f-strings** para imprimir tus tres resultados con el formato: `"Promedio: X kg"`.
5. **🔥 RETO EXTRA:** ¿Qué peso se repite más? Verifícalo visualmente en la lista antes de ejecutar el código.

"""

import statistics

pesos_bebes = [3.2, 2.9, 3.1, 3.2, 4.0, 3.2, 2.8]

# Escribe aquí tu código para el Ejercicio 2.1:
promedio_peso = statistics.mean(pesos_bebes)
print(f"Peso promedio: {promedio_peso} kg")
mediana_peso = statistics.median(pesos_bebes)
print(f"Peso mediana: {mediana_peso} kg")
moda_peso = statistics.mode(pesos_bebes)
print(f"Peso moda: {moda_peso} kg")

"""---

## Parte 3: Exploración con Datos (¿Qué tan dispersos están mis resultados?)

En nuestra sesión teórica aprendimos que el promedio por sí solo puede ser engañoso si tenemos valores extremos. Para conocer la historia completa, necesitamos saber qué tan agrupados o alejados están nuestros datos de ese centro.

Para esto, nuestra caja de herramientas estadísticas tiene una función clave: **`stdev()`** (del inglés *standard deviation* o desviación estándar).

### Ejemplo Comparativo: El engaño del promedio

Imagina que eres un profesor analizando el rendimiento de dos grupos de estudiantes con el **mismo promedio**. Ejecuta este bloque para descubrir la historia oculta:

"""

import statistics

# Grupo A: Todos sacaron la misma calificación
grupo_a = [8, 8, 8, 8, 8]

# Grupo B: Calificaciones extremas (¡pero mismo promedio!)
grupo_b = [10, 6, 10, 6, 8]

# 1. Promedios
print(f"Promedio Grupo A: {statistics.mean(grupo_a)}")
print(f"Promedio Grupo B: {statistics.mean(grupo_b)}")
print("─" * 40)

# 2. Desviación estándar
desv_a = statistics.stdev(grupo_a)
desv_b = round(statistics.stdev(grupo_b), 2)

print(f"Desviación Grupo A: {desv_a}  → Consistente 🎯")
print(f"Desviación Grupo B: {desv_b}  → Impredecible ⚡")
print()
print("Mismo promedio. Historia completamente diferente.")

"""### ¿Qué acaba de pasar?

Si solo miras el promedio, ¡ambos grupos tienen exactamente un 8! Parecen idénticos. Sin embargo, al observar la desviación estándar generada por `stdev()`, la historia cambia radicalmente:

* El **Grupo A** tiene una desviación de `0.0`, lo que significa que no hay variabilidad en absoluto; todos son consistentes.
* El **Grupo B** tiene una desviación mayor (`1.83`), indicando que sus calificaciones saltan constantemente entre la excelencia y la reprobación.

---

### Interpretación Data Science: El impacto en la vida real

¿Por qué esta diferencia matemática es de vida o muerte en tu área de estudio?

* **Para un investigador médico:** Si un nuevo fármaco reduce la presión arterial en 15 puntos en promedio, pero tiene una desviación estándar enorme, el medicamento es impredecible y peligroso. Podría reducirle 30 puntos a un paciente (causando un colapso) y 0 puntos a otro.
* **Para un economista o sociólogo:** Si el ingreso promedio de dos ciudades es de $20,000, pero una tiene una desviación estándar altísima, significa que hay una profunda desigualdad social (unos pocos millonarios frente a muchos ciudadanos en pobreza extrema).

> 🏆 **La regla de oro del Científico de Datos:** Un promedio **sin** su desviación estándar es una historia incompleta.

---

## 🆕 ¿Cómo interpreto el número de la desviación estándar?

El resultado de `stdev()` es un número, pero ¿qué significa exactamente? La clave está en compararlo con el promedio usando esta regla mental:

**Si la desviación estándar es...**

| Relación desv / promedio | Interpretación | Ejemplo |
|---|---|---|
| **Muy pequeña** (< 5% del promedio) | Datos muy consistentes, el promedio es muy confiable | Ruta A del autobús de S9 |
| **Moderada** (5-20% del promedio) | Variabilidad normal, el promedio es útil con precaución | Tiempos de recuperación del paciente |
| **Grande** (> 20% del promedio) | Alta dispersión, el promedio puede engañar | Ingreso en vecindario con millonario |
| **Mayor que el promedio** | Datos extremadamente dispersos, usa la mediana | Tiempos de ambulancia con el caso de 300 min |

> 🔑 **Fórmula mental rápida:** `desv / promedio * 100` = % de variabilidad. Si ese porcentaje es mayor que 20%, desconfía del promedio y reporta también la mediana.

---

### Ejercicio 3.1: Eligiendo al Mejor Proveedor

Imagina que trabajas en logística y debes elegir qué proveedor de transporte contratar. Ambos prometen entregar en un **promedio de 20 minutos**, pero necesitas saber quién es más confiable y consistente.

**Tu tarea:**

1. En la celda de abajo, usa las listas `proveedor_x` y `proveedor_y`.
2. Calcula el **promedio** (`mean`) y la **desviación estándar** (`stdev`) de cada proveedor.
3. Imprime los resultados con `round()` a 2 decimales usando **f-strings**.
4. Calcula el **% de variabilidad** (`desv / promedio * 100`) para cada proveedor y muéstralo.
5. Escribe en un comentario (`#`) cuál proveedor contratarías y por qué.
"""

import statistics

# Tiempos de entrega en minutos de los últimos 5 pedidos
proveedor_x = [20, 21, 19, 20, 20]
proveedor_y = [10, 30, 15, 35, 10]

# Escribe aquí tu código para el Ejercicio 3.1:

# Tu conclusión (escríbela como comentario):
# Contratarías a: _____ porque...

"""---

## Parte 4: Reto Práctico (Control de Calidad)

Para cerrar nuestra sesión de hoy, vamos a poner a prueba su nuevo "ojo clínico" para los datos. Imaginen que están auditando los tiempos de respuesta (en minutos) de un servicio de ambulancias y atención de emergencias.

La mayoría de las llamadas se atienden rápido, pero hubo un caso anómalo donde el sistema falló terriblemente.

**Tus instrucciones paso a paso:**

1. **Calcula el Promedio y la Mediana:** Usa `statistics.mean()` y `statistics.median()`. Imprime ambos con f-strings.
2. **Calcula la Desviación Estándar:** Usa `statistics.stdev()`, redondea a 2 decimales e imprímela.
3. **🔥 RETO EXTRA:** Calcula el **% de variabilidad** (`desv / promedio * 100`). ¿Supera el 20%? ¿Qué significa eso?

"""

import statistics

# Tiempos de respuesta en minutos (¡Ojo con el valor extremo al final!)
tiempos_respuesta = [5, 6, 4, 7, 5, 6, 8, 5, 300]

# ¡Tu turno! Escribe tu código debajo:

# 1. Promedio y Mediana:

# 2. Desviación Estándar:

# 3. RETO EXTRA — % de variabilidad:

"""---

### 🧠 Diagnóstico del Analista (Tu reflexión)

Haz doble clic en esta celda de texto y escribe una breve conclusión respondiendo a lo siguiente:

> *Considerando ese enorme valor extremo (300 minutos): ¿cuál medida (el promedio o la mediana) cuenta la verdadera historia de lo que espera la mayoría de los usuarios al llamar? ¿Qué te dice la enorme desviación estándar sobre la confiabilidad general de este servicio de emergencias? ¿El % de variabilidad que calculaste confirma tu sospecha?*

**(Escribe tu diagnóstico aquí abajo):**

*

---

## 🆕 Puente hacia la Sesión 11: ¿Qué pasa cuando los datos no son perfectos?

¡Excelente trabajo de hoy! Ya dominas las 4 herramientas del módulo `statistics` y sabes interpretar cada resultado. Hasta ahora hemos trabajado con listas **perfectamente limpias**: todos los valores eran números válidos.

Pero en el mundo real, los datos casi nunca llegan así. Ejecuta esta celda y observa qué pasa cuando `statistics.mean()` encuentra una lista con un dato faltante:
"""

import statistics

# Datos del mundo real: un paciente no registró su edad
edades_reales = [25, 30, None, 35, 40]

# ¿Qué pasa si intentamos calcular el promedio?
try:
    promedio = statistics.mean(edades_reales)
    print(f"Promedio: {promedio}")
except TypeError as e:
    print(f"💥 Error: {e}")
    print()
    print("Python no puede sumar números con espacios vacíos (None).")
    print("En la Sesión 11 aprenderemos a DETECTAR y LIMPIAR estos problemas.")
    print("Ese proceso se llama: Limpieza de Datos (Data Cleaning)")